#include <bits/stdc++.h>
using namespace std;
int n, m;
#define maxn 5050
int in[maxn], ou[maxn], f[maxn], ans;
vector<int> a[maxn];
int main() {
    cin >> n >> m;
    for(int i = 1; i <= n; i++) {
        
    }
}